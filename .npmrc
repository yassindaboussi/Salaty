# Skip downloading the Electron binary during CI/CD npm install.
# The binary is not needed for linting, testing, or building in CI.
# Remove this file (or set to false) on developer machines if you need
# to run the app locally without ELECTRON_SKIP_BINARY_DOWNLOAD=1.
electron_skip_binary_download=1
